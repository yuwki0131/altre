# 再帰呼出しがうまく機能していない。

ドキュメントには以下記載があるが、再帰的なスコープ参照ができていない。
再起参照ができるように、alispインタプリタの実装を修正してください。

```
(define (fib n)
  (if (<= n 1)
      n
      (+ (fib (- n 1)) (fib (- n 2)))))

(fib 10) ; => 55
```
